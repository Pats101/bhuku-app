# Pinned to current stable majors; bump deliberately.
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
pydantic==2.7.1
pydantic-settings==2.3.0
python-jose[cryptography]==3.3.0   # Supabase JWT verification
httpx==0.27.0                      # fetch Supabase JWKS

# dev / test
pytest==8.2.1
pytest-asyncio==0.23.7
aiosqlite==0.20.0   # in-memory async DB for fast sync tests
